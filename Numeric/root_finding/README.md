# Root finding

## Target Function

Find `x` such that `f(x) = g(x)`

* `f(x) = exp(x)`
* `g(x) = x + 2`

## Method

Use bisection method

## Build Process

```sh
# Build
cargo build --release

# Run
cargo run --release
```
